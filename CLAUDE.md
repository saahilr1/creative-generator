# Claude Code

The full instructions for this project live in **`AGENTS.md`**. Read it and follow it.
(This file is just a pointer so Claude Code auto-loads the runbook; `AGENTS.md` is the
single source of truth, shared with Codex, Antigravity, Cursor, etc.)
