requests>=2.31.0      # NVIDIA NIM calls (Flux + critic)
playwright>=1.44.0    # headless Chromium render (HTML -> PNG)
pillow>=10.0.0        # downscales the composed image for the NIM vision critic
